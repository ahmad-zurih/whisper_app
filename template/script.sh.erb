#!/bin/bash
# template/script.sh.erb

AUDIO_FILE="<%= context.audio_file %>"
TASK_MODE="<%= context.task %>"
LANGUAGE_MODE="<%= context.language %>"
MODEL_MODE="<%= context.model %>"

SCRIPT_DIR="/storage/research/dsl_shared/solutions/whisper/scripts"

echo "Running Whisper container..."
"$SCRIPT_DIR/transcribe.sh" "$AUDIO_FILE" -t "$TASK_MODE" -l "$LANGUAGE_MODE" -m "$MODEL_MODE"

